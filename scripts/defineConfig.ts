import type { starreadthemeconfig } from './config';

/** 
 * 定义主题配置
 * @param config 传入的主题配置
 * @returns 返回原样配置（带类型检查和提示）
 */
export function StarReadThemeConfig(config: starreadthemeconfig): starreadthemeconfig {
  return config;
}