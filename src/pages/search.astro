---
import NavBar from "../components/NavBar.astro";
import { themeConfig } from '../../starread.config';
const { title } = Astro.props;

// 重定向到主页，因为我们现在使用弹出式搜索
Astro.redirect('/', 301);
---

<html lang="zh-CN" class="min-h-full antialiased">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/png" href={themeConfig.site.favicon} />
    <meta name="generator" content={Astro.generator} />
    <title>{ title || themeConfig.site.title }</title>
    <!-- 如果重定向失败，添加JS重定向作为后备 -->
    <script>window.location.href = '/';</script>
  </head>
  <body class="min-h-screen bg-slate-100 text-gray-900 dark:bg-slate-900 dark:text-neutral-100 transition-colors">
      <NavBar title={title} />
      <main class="container mx-auto px-4 py-16 flex flex-col items-center justify-center text-center">
        <div class="max-w-md">
          <h1 class="text-2xl font-bold mb-4">搜索功能已更新</h1>
          <p class="text-gray-600 dark:text-gray-400 mb-8">我们现在使用弹出式搜索功能，请使用顶部导航栏的搜索按钮或按 <kbd class="px-2 py-1 bg-gray-200 dark:bg-gray-700 rounded mx-1">⌘K</kbd> 打开搜索</p>
          <a href="/" class="inline-flex items-center gap-2 px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 transition-colors">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
            </svg>
            返回首页
          </a>
        </div>
      </main>
    </main>
  </body>
</html>
