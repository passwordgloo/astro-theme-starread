---
import { themeConfig } from '../content/config';
---

<div class="bg-white rounded-lg shadow-sm p-4">
  <div class="flex items-center space-x-3 mb-4">
    <img src={themeConfig.author.avatar} alt={themeConfig.author.name} class="w-10 h-10 rounded-full object-cover" />
    <div>
      <div class="flex items-center space-x-2">
        <svg class="w-4 h-4 text-primary" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
        </svg>
        <h3 class="font-medium">{themeConfig.author.name}</h3>
      </div>
      <p class="text-sm text-gray-500">{themeConfig.author.description}</p>
    </div>
  </div>
  <div class="space-y-2 text-sm text-gray-600">
    <p>B站: {themeConfig.author.social.bilibili}</p>
    <p>QQ: {themeConfig.author.social.qq}</p>
  </div>
</div>